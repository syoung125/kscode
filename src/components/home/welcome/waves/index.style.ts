import styled from "styled-components";

const Wrapper = styled.div`
  position: absolute;
  bottom: 0;

  width: 100%;
  height: 40vh;
`;

const Canvas = styled.canvas`
  width: 100%;
  height: 100%;
`;

export default {
  Wrapper,
  Canvas,
};
